# Spark Cluster Public - Environment Configuration
# Copy this file to .env and configure for your environment
# ============================================================

# =============================================================================
# Node Hostnames/IPs
# =============================================================================
# Replace with your actual node IPs or hostnames
SPARK3_HOST=localhost
SPARK4_HOST=localhost

# =============================================================================
# Spark 3 "Brain" - Primary LLM
# =============================================================================

# Model Configuration
BRAIN_MODEL_ID=your-model-id-here
BRAIN_MODEL_NAME=brain-llm

# vLLM Image (NGC registry requires authentication)
VLLM_IMAGE=nvcr.io/nvidia/vllm:25.11-py3

# vLLM Settings
VLLM_PORT=8000
VLLM_ATTENTION_BACKEND=FLASHINFER
GPU_MEM_UTIL=0.85
MAX_MODEL_LEN=131072

# Quantization (uncomment if your model uses quantization)
# QUANTIZATION_METHOD=modelopt_fp4

# Prompt Registry
PROMPT_PORT=8010
PROMPTS_DIR=./prompts

# HuggingFace Cache
HF_CACHE_DIR=./hf_cache

# =============================================================================
# Spark 4 "Utility Belt" - Specialized Services
# =============================================================================

# Prover Model (math/logic reasoning)
PROVER_MODEL_ID=your-prover-model-id
PROVER_MODEL_NAME=prover
PROVER_PORT=8005
PROVER_GPU_MEM=0.35
PROVER_MAX_LEN=8192

# OCR Model (disabled by default - may require newer driver)
# OCR_MODEL_ID=your-ocr-model-id
# OCR_PORT=8006
# VLLM_IMAGE_OCR=nvcr.io/nvidia/vllm:25.12.post1-py3

# Whisper Speech-to-Text
WHISPER_MODEL_ID=openai/whisper-large-v3-turbo
WHISPER_IMAGE=fedirz/faster-whisper-server:latest-cuda
WHISPER_PORT=8007

# Weather Service
WEATHER_PORT=8008
WEATHER_BACKEND_URL=http://localhost:6000

# A2A Gateway
A2A_GATEWAY_PORT=9000

# =============================================================================
# MCP Server
# =============================================================================
MCP_HOST=0.0.0.0
MCP_PORT=5050

# Tool Server URLs (configure based on your deployment)
WEATHER_URL=http://localhost:6000
MEDIA_URL=http://localhost:5001
KB_URL=http://localhost:8002
PDF_URL=http://localhost:8001
PROVER_URL=http://localhost:8005
A2A_GATEWAY_URL=http://localhost:9000

# =============================================================================
# Prompt Browser (Streamlit)
# =============================================================================
REGISTRY_URL=http://localhost:8010
OPENWEBUI_URL=http://localhost:8080
